apiVersion: v1
kind: Service
metadata:
  name: job-orchestrator-postgres-service
spec:
  selector:
    app: job-orchestrator-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP