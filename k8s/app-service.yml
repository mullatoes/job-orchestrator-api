apiVersion: v1
kind: Service
metadata:
  name: job-orchestrator-api-service
spec:
  selector:
    app: job-orchestrator-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: NodePort